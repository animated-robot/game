class Game {
  /**
   * Initialise the game
   * @param {GameContainer} container
   */
  init (container) {}

  /**
   * Render the game
   * @param {GameContainer} container
   * @param {Graphics} g
   */
  render (container, g) {}

  /**
   * Update the game logic
   * @param {GameContainer} container
   * @param {number} delta
   */
  update (container, delta) {}
}

export default Game
